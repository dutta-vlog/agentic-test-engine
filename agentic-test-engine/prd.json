{
  "feature_name": "AI-Driven Intelligent Alerting Engine",
  "description": "System routes high-priority security alerts based on severity and source infrastructure.",
  "requirements": [
    {
      "req_id": "REQ-001",
      "action": "POST /api/v1/alerts",
      "rules": "If severity is 'CRITICAL' and source is 'GCP', route to Slack channel #security-gcp-critical within 2 seconds."
    },
    {
      "req_id": "REQ-002",
      "action": "POST /api/v1/alerts",
      "rules": "If severity is 'LOW', batch alerts and store in DB; do not trigger real-time webhooks."
    }
  ]
}
